import './styles/global.css';

// This component exists purely to ensure the global stylesheet is included
// once in the React tree. It renders nothing by itself.

export default function GlobalStyles() {
  return null;
}